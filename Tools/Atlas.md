Given a set of training problems, an Abstraction-Guided Synthesizer, and a DSL, it returns an abstract domain (in the form of predicate templates) and the corresponding transformers.

Uses [[Z3]], [[JLinAlg]], [[Blaze]]